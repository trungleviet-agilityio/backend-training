/*
Users interfaces are used to define the interfaces for the users.
*/

/*
IUser is the interface for the user.
*/
export interface IUser {
  id: number;
  name: string;
  email: string;
  password: string;
}
